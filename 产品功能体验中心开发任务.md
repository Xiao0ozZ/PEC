# 产品功能体验中心开发任务

## 版本记录

| 版本 | 日期       | 编辑  | 变更摘要                                                                 | 依据                                                                                 |
| ---- | ---------- | ----- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| v1.0 | 2026-08-18 | Codex | 建立本地运行产品化、低门槛使用和 AI 产品交付上下文能力的唯一后续开发台账 | 项目负责人确认的三阶段方向、`ARCHITECTURE.md`、`README.md`、当前代码和本轮自动化验证 |

本文是“产品功能体验中心”后续能力建设的唯一开发任务台账。产品定位和使用方式以 `README.md` 为准；技术分层以 `ARCHITECTURE.md` 为准；React 迁移边界以 `REACT_MIGRATION_PLAN.md` 和 `REACT_MIGRATION_VERIFICATION.md` 为准；项目包规则以 `PROJECT_PACKAGE_GUIDE.md` 为准。

> 文档权限：开发 AI 仅可修改自己正在执行任务的“状态”单元格，并在第 9 节末尾追加一条符合格式的进度记录。任务定义、优先级、实施要求、验收标准、执行顺序、非目标和本文其他内容默认锁定。任何新增、删除、合并、拆分或改写任务，必须先获得项目负责人明确确认。

## 1. 基线判定规则

- 每次开发前重新检查当前分支、Git 状态、目录结构、实际代码和适用测试，不在正文写死容易过期的 commit 或进程状态。
- `projects/` 是本地项目资料，默认不得修改、删除、移动或加入 Git；只有项目负责人明确指定的项目操作可以写入对应项目包。
- 项目文件夹中的 HTML、Markdown、JSON、资源和配置继续作为事实来源，不迁移到数据库。
- 页面存在、接口返回 200、单元测试通过或开发者自报，不能单独证明局域网权限、原子写入、备份恢复、外部目录、生产构建或完整回环可用。
- 已完成工作、验证命令、结果与剩余风险只记录在第 9 节；历史通过记录不能替代当前任务复核。
- 浏览器视觉与交互验收需项目负责人明确授权；未执行时必须写明“未做浏览器验收”，不得用代码检查代替视觉结论。

## 2. 产品与架构结论

目标稳定架构：

```text
React 界面
    ↓
platform-client
    ↓
platform-server（独立本地服务）
    ↓
project-core + platform-transfer
    ↓
HTML / Markdown / JSON / 项目文件夹
```

架构约束：

- Vite 只承担前端开发、构建和确有必要的 Vue 页面编译，不再作为正式运行时的唯一后端。
- `platform-client` 是浏览器端唯一平台数据访问入口；React 页面不得直接解析或改写项目文件。
- `platform-server` 负责 HTTP、权限、请求体限制、路径边界和只读分享；领域规则下沉到 `project-core` 或 `platform-transfer`。
- 项目包文件是唯一事实来源。SQLite 只能用于全文搜索索引、缓存和操作记录，不能替代项目文件。
- 本机允许经过授权的写操作；局域网其他设备默认只读，不能通过接口修改项目、路由、PRD 关联或页面文件。
- Electron/Tauri 仅作为未来桌面包装，不进入当前核心架构；Node 服务必须可以独立运行。

当前非目标：

- 不建设云端 SaaS 后端、多人实时协同数据库或账号体系。
- 不自动把项目包迁移到数据库。
- 不让 AI 未经确认直接修改 PRD、HTML、路由或项目配置。
- 不因本地服务建设重写实际项目页面内容、模拟数据和视觉样式。
- 不在 React 覆盖和观察期完成前删除 Vue 回退代码。

## 3. 完成定义

任务只有同时满足以下条件才能标记 `Done`：

- 实施范围、权限、异常、回退和非目标明确；
- React、`platform-client`、本地服务和领域包的接口契约一致；
- 所有文件路径经过白名单、相对路径和真实路径边界校验；
- 关键写入使用原子替换，涉及多文件时具备备份、回滚或明确的失败恢复策略；
- 本机写入和局域网只读均有自动测试，不能只验证正常本机请求；
- 正常、无效输入、超限、目标不存在、权限拒绝和中途失败至少覆盖适用场景；
- 不修改未被任务明确选中的 `projects/` 资料；
- 适用的 Lint、类型检查、单元测试、项目校验、生产构建和 `git diff --check` 通过；
- 文档、命令和真实运行方式一致，不把“基础可用”写成“生产就绪”。

状态只能使用：`Done`、`In Progress`、`Pending`、`Reserved`。

## 4. P0：本地运行能力产品化

P0 目标：让平台脱离 Vite 开发服务器后，仍能安全、稳定地读取和管理本地项目资料。

| ID      | 状态     | 任务                     | 实施要求                                                                                                                                  | 验收                                                                                                       |
| ------- | -------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| RT-01   | Done     | 框架无关平台客户端       | `platform-client` 统一项目、HTML、PRD、关联、设置、路由和页面传输的请求、错误与响应归一化；支持 `development`、`local`、`static` 三种模式 | React 不直接调用 Vite 插件或读取项目文件；静态模式写操作在请求前拒绝；本地模式走独立服务接口               |
| RT-02   | Done     | 独立 Node 读取服务       | `platform-server` 提供健康检查、项目清单、HTML 目录与内容、资源、PRD、平台设置和关联配置读取，并承载 React `dist`                         | 不启动 Vite 即可读取样例项目、HTML 和 PRD；直接访问前端路由可回退到 React 入口；自动测试通过               |
| RT-03   | Done     | 本机写入与局域网只读     | 服务默认只读；正式本地命令显式开启写入；写接口根据真实客户端地址只允许服务主机访问                                                        | 本机保存成功；模拟局域网地址返回 403；只读模式返回稳定错误；健康接口报告实际读写模式                       |
| CORE-01 | Done     | 项目领域能力下沉         | 将项目扫描、HTML 扫描、项目新建编辑、文档根目录和挂载解析继续从 `plugins/` 下沉到 `project-core`；插件只保留 Vite 适配                    | `platform-server`、CLI 和 Vite 插件调用同一领域函数；核心包不依赖 Vite、HTTP、React 或 Vue；重复规则被删除 |
| XFER-01 | Done     | 页面传输独立包           | 将页面定义、路由、分组、备份、HTML 检查、导入、回导和导出编排迁入 `platform-transfer`；旧插件路径保留兼容入口                             | 独立服务直接调用 transfer 包；路由读取和 HTML 检查不加载 esbuild；页面传输回归测试与生产构建通过           |
| XFER-02 | Done     | 导出编译边界收口         | 轻量 HTML 导出不得加载 Vite；只有必须编译的 Vue 页面才按需加载 Vue/Vite；继续减少 transfer 包对插件目录的依赖                             | HTML 轻量导出、回导和直读无需 Vite；Vue 编译路径有明确提示和失败信息；全选与单页导出结果一致               |
| SAFE-01 | Done     | 路径白名单与写入边界     | 统一项目根、挂载目录、文档、HTML、导出和资源的路径解析；拒绝 `..`、绝对路径、越界符号链接和非白名单扩展名                                 | 目录穿越、符号链接越界、错误项目 ID 和非白名单文件均被拒绝；合法外置目录正常读取                           |
| SAFE-02 | Done     | 原子写入、备份和错误恢复 | JSON 使用原子替换；项目配置、路由、分组、页面导入和恢复形成统一备份策略；多文件操作失败时恢复原状态                                       | 故障注入后原文件完整；不存在半写定义、孤立页面或丢失备份；恢复动作有明确结果和可追溯记录                   |
| RUN-01  | Done     | Node 本地运行产品化      | `npm run serve:local` 成为正式本地运行入口；补配置检查、端口冲突、目录缺失、构建缺失和退出处理                                            | 一个命令可启动；错误信息可执行；Ctrl+C 正常退出；本机和局域网地址提示准确；无需 Vite 开发服务器            |
| IDX-01  | Reserved | SQLite 辅助索引          | 仅在项目规模证明需要后，引入 SQLite 保存全文索引、缓存和操作记录；所有数据可从项目文件重建                                                | 删除 SQLite 后平台可自动重建；项目内容不依赖数据库恢复；未达到性能阈值前不实施                             |

P0 中 `CORE-01`、`SAFE-01`、`SAFE-02` 和 `RUN-01` 未完成前，不宣称独立服务已经达到稳定桌面产品运行标准。

## 5. P1：降低产品人员使用门槛

P1 目标：让用户把时间用在项目内容上，而不是配置、命令和目录维护上。

| ID        | 状态        | 任务                  | 实施要求                                                                                             | 验收                                                                                                 |
| --------- | ----------- | --------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| UX-01     | Done        | 首次启动向导          | 检查构建、项目目录、项目挂载、端口和分享模式；提供“打开现有项目”“创建示例”“暂不配置”                 | 新用户无需阅读命令文档即可进入空平台或加载项目；取消向导不写入项目资料                               |
| MOUNT-01  | Done        | 选择现有项目文件夹    | 复用 `project-mounts.local.json`；选择包含合法 `project.json` 的目录后完成本地挂载；项目文件保持原位 | 不复制、不改写原项目即可显示；无效目录给出具体问题；挂载配置被 Git 忽略；可安全取消挂载              |
| HEALTH-01 | Done        | 项目健康检查中心      | 汇总 Manifest、页面定义、HTML 来源、PRD、关联、路由、资源、挂载和 Schema 状态；复用 CLI/核心校验     | 一个页面看到错误、影响对象和修复建议；重新扫描后状态更新；错误不被“项目不可用”笼统覆盖               |
| SHARE-01  | Done        | 一键局域网只读分享    | UI 控制服务监听地址和只读分享状态，展示可访问 URL；写权限仍只属于服务主机                            | 开启后同网设备可查看项目、原型、PRD 和评审文件；远程写接口全部 403；关闭后不再监听局域网地址         |
| REVIEW-01 | Done        | 一键生成独立评审包    | 复用现有 `build-review` 和页面导出；明确快照时间、项目、页面、PRD 与关联范围                         | 无 Node 环境可查看；包内链接、参数、资源、PRD 和页面跳转有效；生成过程不修改源项目                   |
| SCHEMA-01 | Done        | Schema 升级与迁移提示 | 扫描项目包版本；先预览迁移内容和备份位置；只有用户确认后写入                                         | 旧版本可继续只读；升级前展示差异；失败自动恢复；迁移测试覆盖空包、旧包和重复执行                     |
| VUE-01    | Reserved    | 删除 Vue 回退平台     | 仅在 React 功能完全覆盖、观察期结束且项目负责人再次明确确认后执行                                    | 正式依赖不含 Vue/Element Plus；旧 URL、项目包、HTML、PRD 和导入导出仍可用；存在可恢复的最终 Vue 基线 |

## 6. P2：AI 产品交付上下文层

P2 目标：让 Codex、Claude 等工具可靠理解项目、页面、PRD 和关联，而不是无上下文地生成页面。

| ID         | 状态        | 任务               | 实施要求                                                                                                               | 验收                                                                                             |
| ---------- | ----------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| MCP-01     | Done        | 本地 MCP Server    | 基于 `project-core` 和 `platform-transfer` 提供只读工具：列出项目、客户端、页面、PRD、关联和健康问题；写工具默认不开放 | AI 工具无需解析仓库即可取得结构化上下文；远程网络不可访问；不返回工程外文件或敏感本地路径        |
| CTX-01     | Done        | 当前页面完整上下文 | 输出项目、客户端、路由、页面来源、主题、页面级 PRD、组件级关联、关联章节和必要文档摘要                                 | 同一页面在 UI 导出、CLI 和 MCP 中结果一致；缺失内容明确标记，不编造关联                          |
| IMPACT-01  | Pending     | PRD 变更影响分析   | 对比文档版本、章节锚点和页面关联，生成受影响页面、失效锚点和待复核项                                                   | 修改 PRD 后可列出受影响页面及依据；没有关联时不猜测；结果可导出但不自动改页面                    |
| TRACE-01   | Done        | 追溯与孤立项检查   | 检查未关联页面、孤立 PRD、失效路由、失效章节、缺失源文件和重复页面标识                                                 | 每个问题包含项目、客户端、页面或文档路径、严重级别和可执行建议；误报有回归测试                   |
| SUGGEST-01 | Pending     | PRD 页面修改建议   | 根据 PRD 与当前页面上下文生成修改建议、候选关联和验收点；所有修改必须由人确认                                          | 默认只生成建议；确认前不写文件；建议显示依据、影响文件和风险；拒绝后不留下改动                   |
| HTML-01    | Done        | HTML 模板智能检查  | 检查模板协议、页面结构、交互源码、主题变量、资源、链接、查询参数、菜单外壳和回导兼容性                                 | 检查结果区分错误与建议；能覆盖单页、互链、多参数、弹窗和主题色；相同 HTML 在导入与直读中结论一致 |

P2 不得绕过 P0 的路径、权限和事实来源边界。MCP、AI 上下文和建议能力只能调用核心服务，不能自行扫描任意磁盘目录或直接改写文件。

## 7. 执行顺序与暂停点

```text
P0：CORE-01
  ↓
P0：SAFE-01 + SAFE-02
  ↓
P0：XFER-02 + RUN-01
  ↓
P1：UX-01 + MOUNT-01 + HEALTH-01
  ↓
P1：SHARE-01 + REVIEW-01 + SCHEMA-01
  ↓
P2：CTX-01 + TRACE-01 + HTML-01
  ↓
P2：MCP-01
  ↓
P2：IMPACT-01 + SUGGEST-01
  ↓ 观察期与项目负责人明确确认
P1：VUE-01
```

执行规则：

1. 同一时间优先处理最高优先级中第一个未达到 `Done` 且前置满足的任务。
2. `IDX-01` 不阻塞其他任务；只有出现可复现的搜索或索引性能问题并确认阈值后才启动。
3. Electron/Tauri 不在本台账当前执行范围；本地服务稳定后另建明确任务，不借当前任务顺手接入。
4. `MCP-01` 启动前，核心读取接口和路径白名单必须稳定；AI 能力不得复制一套项目解析逻辑。
5. `VUE-01` 必须单独获得项目负责人确认；其他任务完成不能自动触发删除 Vue。
6. 每个任务完成后交付改动文件、实际验证、剩余风险、回退方式和是否建议进入下一任务。

## 8. 标准验收命令

基础门禁：

```powershell
npm run lint:core
npm run typecheck
npm run test:unit
npm run project -- validate --projects-root examples
npm run build
git diff --check
```

独立服务任务还必须验证：

```text
1. 不启动 Vite，独立 Node 服务可以读取样例项目、HTML、PRD 和路由。
2. 本机写入成功，局域网模拟地址写入返回 403。
3. 请求体超限、无效 JSON、非法路径和缺失项目返回稳定错误。
4. 写入失败后原文件完整，临时文件和半写目录被清理。
5. 服务停止后端口释放，不残留错误项目进程。
```

涉及 UI 的任务必须另外记录是否获得浏览器验收授权；未授权时只报告代码、类型、测试和构建结果。

## 9. 台账维护与开发进度记录

维护规则：

- 开发前选择一个任务 ID，不同时扩展多个不相干任务。
- 开发 AI 只能修改所执行任务的状态单元格，并在本节末尾追加进度；不得修改任务定义、优先级、验收和执行顺序。
- 未满足全部验收时不得标记 `Done`；只能记录 `部分完成` 或 `待验收`。
- 新需求、任务拆分或优先级变化只向项目负责人报告；获得明确确认后才更新本文版本记录和任务表。
- 进度记录只追加、不覆盖。时间使用 Asia/Shanghai；引用他人结果必须标记“自报/未复核”。
- 不在本文记录账号、密码、Token、密钥、真实客户数据或外部项目包绝对路径。

| 时间             | 执行者 | 任务 ID | 实际开发工作                                                                                                                                         | 验证证据                                                                                                                                   | 结果     | 未完成与风险                                                                      |
| ---------------- | ------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------- |
| 2026-08-18 17:27 | Codex  | RT-01   | 建立框架无关 `platform-client`，支持开发、本地服务和静态三种 API 模式；React 本地运行标记接入统一客户端                                              | `npm run lint:core`、`npm run typecheck:react`、`npm run test:unit`、`npm run build`、`git diff --check` 均通过                            | 完成     | 静态部署仍保持只读，属于设计边界                                                  |
| 2026-08-18 17:27 | Codex  | RT-02   | 新增独立 Node 服务，承载 React 构建、项目、HTML、资源、PRD、关联、设置、路由和页面传输接口                                                           | 28 个测试文件、118 个测试通过；独立服务测试覆盖读取、设置和关联配置                                                                        | 完成     | 正式桌面产品运行标准仍受 `CORE-01`、`SAFE-01`、`SAFE-02`、`RUN-01` 约束           |
| 2026-08-18 17:27 | Codex  | RT-03   | 增加 `writeEnabled`、本机地址校验、请求体限制；正式本地命令为本机可写、局域网只读                                                                    | 自动测试覆盖本机成功、远程 403、只读拒绝和超限拒绝                                                                                         | 完成     | 尚未提供 UI 一键分享开关，归 `SHARE-01`                                           |
| 2026-08-18 17:27 | Codex  | XFER-01 | 将页面传输实现迁入 `packages/platform-transfer`，独立服务直接调用，旧插件路径保留兼容导出；Vue/Vite 编译改为按需加载                                 | 页面传输 12 项回归、独立服务路由与 HTML 检查回归、全量 118 项测试和生产构建通过                                                            | 完成     | HTML 扫描仍位于插件目录；Vue 页面导出仍需按需加载 Vite，归 `CORE-01` 与 `XFER-02` |
| 2026-08-18 17:27 | Codex  | SAFE-02 | 平台设置、页面级 PRD 关联和组件级 PRD 关联改用 JSON 原子写入；页面与分组继续复用现有备份恢复                                                         | 自动测试证明无效关联写入后原文件不变；`git diff --check` 通过                                                                              | 部分完成 | 项目配置、路由与页面导入仍需统一多文件事务和故障注入，不得标记完成                |
| 2026-08-18 17:50 | Codex  | CORE-01 | 将 HTML 扫描、内容区处理、导出格式识别和项目新建编辑迁入 `project-core`；服务、CLI、传输包和 Vite 插件统一复用核心实现；删除插件内重复校验与转换规则 | `npm run lint:core`、`npm run typecheck:react`、样例项目校验、29 个测试文件共 123 项单测、`npm run build`、`git diff --check` 均通过       | 完成     | 未做浏览器验收；构建仍有既存的大 chunk 警告，不属于本任务验收范围                 |
| 2026-08-18 18:16 | Codex  | SAFE-01 | 在 `project-core` 增加真实路径读写解析；项目、文档、HTML、资源、备份和导出复用路径白名单；保留显式配置的绝对外置目录，并阻止从允许根经目录联接逃逸   | 自动测试覆盖 `..`、绝对请求路径、非白名单扩展名、目录联接越界、错误项目 ID 和合法外置 PRD/HTML 读取；全量 132 项单测通过                   | 完成     | 未做浏览器验收；显式外置目录本身属于允许根，不按非法绝对路径处理                  |
| 2026-08-18 18:16 | Codex  | SAFE-02 | 增加通用原子文件写入和文件事务回滚；项目编辑、路由、分组、页面导入、恢复及备份元数据接入；失败回传回滚结果，成功恢复记录 `restoredAt`                | 故障注入验证原子替换失败、项目 Manifest 失败、页面定义失败和恢复元数据失败均保持原状态；Lint、类型检查、样例校验、132 项单测和生产构建通过 | 完成     | 构建仍有既存的大 chunk 警告；未做浏览器验收                                       |
| 2026-08-18 18:26 | Codex  | XFER-02 | 为导出编译器增加独立按需加载器；轻量 HTML 导出明确跳过编译加载，Vue 页面编译失败提供页面名称、错误代码和安装建议；transfer 包不依赖插件目录          | 自动测试断言轻量 HTML 编译加载器调用 0 次、Vue 页面仅按需调用；单页、多页、回导和直读回归及全量 139 项单测、生产构建通过                   | 完成     | 多页面 ZIP 仍按既定边界仅支持 Windows；未做浏览器验收                             |
| 2026-08-18 18:26 | Codex  | RUN-01  | 新增独立服务运行时，覆盖参数解析、构建与目录预检、挂载校验、端口错误、本机与局域网地址提示、只读模式和信号退出；补充 CLI 帮助与 README               | `serve:local --help`、运行时故障测试、真实 6288 启动、健康接口和 Ctrl+C 停止通过；Lint、类型检查、139 项单测和生产构建通过                 | 完成     | 实际验证未启动浏览器；6288 临时服务已停止，无残留监听                             |
| 2026-08-18 18:50 | Codex  | UX-01   | 增加首次启动向导与统一工作区状态接口；空工作区可打开现有项目、安装脱敏示例或仅在浏览器中暂时跳过；远程只读访问不显示配置向导                         | Lint、React 类型检查、样例校验、141 项单测和生产构建通过；Vite 6291 实测工作区接口正常                                                    | 完成     | 未做浏览器视觉验收；系统文件夹选择器通过可替换依赖和接口测试，未人工操作系统弹窗   |
| 2026-08-18 18:50 | Codex  | MOUNT-01 | 扩展本机挂载 Schema 支持完整项目根目录；项目扫描、资源、HTML、PRD、配置和页面传输统一解析外部项目；增加选择、预检、挂载和安全取消挂载接口及 UI        | 临时外部目录自动测试证明无需复制即可扫描、健康检查和取消挂载，取消后源 `project.json` 仍存在；全量门禁通过                               | 完成     | 未做浏览器视觉验收；实际项目包未修改，`projects/` 无 Git 改动                     |
| 2026-08-18 18:50 | Codex  | HEALTH-01 | 新增核心健康报告和 React 健康检查中心，按项目汇总 Manifest、Schema、路由、HTML、PRD、关联、资源与挂载，问题包含影响对象、级别和修复建议；CLI 复用同一实现 | 独立服务和 Vite 接口测试通过；当前本地资料扫描返回 2 个项目、5 个既存 HTML 规范错误和 102 条提醒；Lint、类型检查、141 项单测和构建通过     | 完成     | 未做浏览器视觉验收；现有项目问题仅报告未修改；构建仍有既存大 chunk 警告           |
| 2026-08-23 03:25 | Claude | SHARE-01  | 新增 `platform-server/src/addresses.js` 收口局域网地址枚举与 URL 格式化（同时消除 runtime.js 与 index.js 的循环依赖风险）；服务端增加 `/__platform/share` 读写接口与 `setShareEnabled`，开启分享时为每个局域网 IPv4 地址增设独立监听、关闭时立即释放，主监听全程不动以避免在请求处理内关闭自身连接导致 `server.close()` 永不回调；健康与 bootstrap 接口同步报告分享状态；`platform-contracts` 增加 `normalizeShareStatus` 与类型、`platform-client` 增加 `loadShareStatus`/`saveShareEnabled`；控制台新增 `ShareControlPanel` 开关与 URL 复制。顺带修复 `project-definitions.js` 中遗留的未使用变量 `clientIds`（此前 `lint:core` 在 HEAD 上即失败） | 新增 `tests/unit/platform-server-share.spec.js` 11 项：默认不分享、健康接口报告、开启后真实监听局域网并可读取项目清单、关闭后局域网地址不再监听、局域网客户端 `canControl:false` 且切换返回 403、只读模式 403、非布尔 400、错误方法 405、体积超限 413、以 `0.0.0.0` 启动时返回 409、关闭服务同时释放分享监听。真实运行 `npm run serve:local -- --port 6299` 全链路验证：开启前局域网不可达 → 开启后 `192.168.0.103:6299` 可读项目清单 → 局域网 `POST /__platform/settings` 与 `POST /__platform/share` 均 403、本机 POST 200 → 关闭后局域网不可达而本机正常 → 停止后端口 6299 已释放且无残留进程。`npm run lint:core`、`npm run typecheck:react`、`npm run test:unit`（34 文件 153 项）、`npm run project -- validate --projects-root examples`、`npm run build`、`git diff --check` 全部通过 | 完成     | **未做浏览器验收**（未获授权，仅验证代码、类型、测试、构建与接口行为）；分享开启后新插入的网卡需重新切换才会被纳入监听；以 `--host 0.0.0.0` 启动时界面开关按设计不可用，需改用启动参数；构建仍有既存大 chunk 警告 |
| 2026-08-23 04:15 | Claude | REVIEW-01 | 新增 `packages/project-core/src/review-snapshot.js`（`createReviewSnapshotManifest`），把评审快照范围收口为领域能力：输出快照时间、`base`、只读标记，以及按项目展开的客户端、页面（含 `route` 跳转路径与来源文件）、PRD 文档、页面级与组件级关联，并给出合计 `scope` 与被排除的无效项目包；`build-review` 改为复用该函数，不再在 CLI 内联拼装清单。修复两处使 `build-review` 在当前环境不可用的真实缺陷：（1）`runNpm` 用 `spawn('npm.cmd')`，Node 20.12 起因 CVE-2024-27980 修复直接抛 `EINVAL`，改为以 `process.execPath` 运行本地 `vite/bin/vite.js`，不使用 `shell:true` 以避免参数注入；（2）Vite 把相对 `--outDir` 解析到 config 的 `root`（apps/platform-react），与清单写入路径不一致，改为统一解析绝对路径。顺带合并 `project-cli.mjs` 中对同一模块的重复 import | 新增 `tests/unit/review-snapshot.spec.js` 7 项：只读标记与固定快照时间、页面/PRD/关联范围与 `route` 正确性、`scope` 合计等于各项目累加、**生成过程逐字节不修改源项目**、相同输入结果稳定可重复、空目录返回空范围、关联配置损坏时按空范围记录且不中断。真实运行 `build-review --base /review/ --out-dir …` 三次成功：产出 102 个页面与 68 份 PRD，清单交叉核对包内文件 **102/102 页面、68/68 文档全部存在**；以纯静态托管（无平台逻辑）验证 **23/23 页面与 PRD 返回 200**（含非 ASCII 中文路径）、入口 HTML/JS/CSS/manifest 均 200；对 `projects/` 与两处外置 PRD/原型根目录共 **664 个源文件**做构建前后比对，结果完全一致。`npm run lint:core`、`npm run typecheck:react`、`npm run test:unit`（35 文件 161 项）、`npm run project -- validate --projects-root examples`、`npm run build`、`git diff --check` 全部通过 | 完成     | **未做浏览器验收**（未获授权，仅验证代码、类型、测试、构建与静态托管下的 HTTP 行为）；平台使用 History 路由，深链直达需静态托管配置 `index.html` 回退，已在 README 说明，从首页点击进入不受影响；本任务保持 `build-review` 单命令入口，未新增 UI 触发按钮（服务端触发需在 HTTP 请求内拉起完整 Vite 构建，与 `platform-server` 只负责 HTTP/权限/边界的分层约束冲突）——如需界面按钮请项目负责人确认后另立任务；示例项目存在指向 `admin/overview` 的组件级关联但该页面不存在，属既存数据问题，归 `TRACE-01`；构建仍有既存大 chunk 警告 |
| 2026-08-23 04:35 | Claude | SCHEMA-01 | 扩展 `migrations.js`：把「缺少 schemaVersion 的早期项目包」定义为 0 版（`LEGACY_PROJECT_SCHEMA_VERSION`）并注册 0→1 迁移器——此前 `migrations` 注册表为空且 `CURRENT_PROJECT_SCHEMA_VERSION=1`，导致验收要求的「旧包」场景在代码里根本不存在、无法测试；新增 `readProjectSchemaVersion`、`hasProjectMigrationPath`、`describeProjectSchemaVersion` 做状态判定。新增 `packages/project-core/src/schema-migration.js`：`scanProjectSchemaVersions` 只读 `project.json` 扫描全部项目包（版本过旧/过新/非法/不可读都能列出，不被笼统归为「项目不可用」）、`diffProjectManifest` 生成逐字段差异、`planProjectMigration` 产出版本区间+差异+备份位置且不写盘、`applyProjectMigration` 先备份到 `<项目根>/.backups/schema/<标识>/`（含 `metadata.json`）再原子替换，全程包在 `withFileRollback` 内。CLI `migrate` 重写为「无 --project 即扫描 / 指定项目则展示差异与备份位置 / 只有 --write 才写回」，并补 `--scan`、`--json`；顺带删除 `migrateProjectManifest` 遗留的未使用 import | 新增 `tests/unit/schema-migration.spec.js` 13 项：版本判定（缺字段=0、非法返回 null、四种状态分类）、0 版迁移补标记且不丢字段、版本过高拒绝、差异区分新增/删除/修改/嵌套、**空项目目录**返回空结果、同时列出当前/旧/过高/不可读四类、**预览不修改文件且不创建备份**、确认后写入并留下可追溯备份与 metadata、**重复执行**返回 `MIGRATION_NOT_APPLICABLE` 且文件不变、**故障注入后自动恢复**（project.json 逐字节不变、状态仍为 upgradable）、非法项目 ID 与不可迁移状态被拒。真实 CLI 全流程验证：扫描现有 3 个项目包均为 v1；构造 v0 旧包后 `migrate --scan` 标记「可升级」→ 预览打印 `+ schemaVersion = 1` 与备份路径且文件未变、无备份目录 → `--write` 写回并生成 `.backups/schema/v0-to-v1/{project.json,metadata.json}`，备份内容与原文件一致 → 再次 `--write` 提示「已是当前版本，未修改任何文件」。`npm run lint:core`、`npm run typecheck:react`、`npm run test:unit`（36 文件 174 项）、`npm run project -- validate --projects-root examples`、`npm run build`、`git diff --check` 全部通过 | 完成     | **未做浏览器验收**（未获授权；本任务只提供 CLI 入口，未在界面接入迁移向导，如需 UI 请项目负责人确认后另立任务）。「旧版本可继续只读」按「迁移前不改动磁盘、旧包原样保留可读」实现：`project-manifest.js` 仍要求运行时 `schemaVersion` 等于当前版本，因此 v0 包在完成迁移前不会进入正式运行范围，仅在版本扫描与健康检查中可见；若需要 v0 包直接参与运行需项目负责人确认后调整校验规则。0→1 迁移器按「早期包仅缺版本标记、字段结构与 v1 一致」实现，若后续发现真实的 v0 字段差异需补充迁移逻辑与兼容测试。构建仍有既存大 chunk 警告 |
| 2026-08-28 18:27 | Claude | TRACE-01 | `ai-context.js` 新增两类追溯检查：（1）`duplicate-page-identity`——同一客户端内合并后的页面清单（page-definitions + HTML 扫描）出现重复 path 或重复 name 时报 error；（2）`missing-page-source`——新增可选输入 `sourceAvailability`（pageKey → 布尔），调用方显式验证为 false 时报 error，不传或未知时跳过以兼容浏览器侧调用（浏览器无法探测文件存在性，不猜测）。全部 issue 类型补齐 `suggestion` 可执行建议字段（含既有 8 类），追溯报告行内 issue 同步携带。CLI `loadContextInput` 新增 `resolveSourceAvailability`：HTML 来源按页面登记的 `sourceRoot` 匹配 `scanHtmlPrototypePages` 返回的来源目录（与扫描器同一套解析，覆盖外部挂载根），模板托管页面回退包内 `html-pages/{clientId}`，Vue 页面校验 `views/` 与兼容 `legacyViewRoot`；真实排查中发现首版只查包内目录导致 rimo-rental 60 个外部挂载页面全部误报缺失，改为复用扫描目录后归零 | 新增回归测试 5 项：重复 path 报错、仅在调用方显式验证时报缺失源文件、全部 issue 带 suggestion、`sourceAvailability` 透传。真实运行 `trace --project rimo-rental`：60 页、36 关联、0 error、24 条既存 page-without-prd 提醒，全部 issue 含 suggestion；`project -- health` 全项目 0 误报。`npm run lint:core`、`npm run typecheck`、`npm run test:unit`（37 文件 190 项）、`npm run project -- validate --projects-root examples`、`npm run build`、`git diff --check` 全部通过 | 完成 | **未做浏览器验收**（纯领域层与 CLI，无界面改动）；浏览器端 `context-loader` 不传 `sourceAvailability`，UI 侧缺失源文件可见性仍由项目健康检查页承担，如需 UI 直显需服务端轻量接口，请负责人确认后另立任务 |
| 2026-08-28 18:27 | Claude | HTML-01 | `html-preflight.js` 新增 6 项检查并区分错误/提醒：（1）`broken-relative-link`（error）——新增可选输入 `knownFiles`，相对 `.html` 互链按当前文件目录解析后必须在已知文件集合中，查询参数与 Hash 剥离后只验路径存在性（覆盖多参数场景），未提供集合时跳过（导入粘贴内容无目录上下文）；（2）`duplicate-sidebar`（error）——侧栏外壳重复，与既有 duplicate-topbar 对齐，嵌套的 sidebar+navigation 计一个外壳；（3）`dialog-outside-overlays`（error，仅模板页）——`el-dialog`/`el-drawer` 出现在 PAGE_OVERLAYS 编辑边界之外；（4）`location-navigation`（提醒）——`window.location` 跳转应改用标准相对链接；（5）`unsupported-export-format`（error）——Manifest `exportFormat` 不在支持列表，回导会被拒绝；（6）`duplicate-manifest-page-key`（error，健康检查分组级）——同来源目录多个页面共用同一 pageKey。接线：`project-health.js` 按来源目录构建文件集合传入 knownFiles 并逐项目聚合 pageKey 重复；CLI `preflight` 默认以文件同目录兄弟页面做互链校验。真实模板（templates/html-prototype-page.html）直读与导入双路验证为 valid，期间修复 sidebar+navigation 重复计数误报 | 新增回归测试 7 项：重复侧栏与非法 exportFormat 拒绝、模板弹窗越界报错/区域内不报、window.location 走提醒不阻断、互链存在性（含多参数链接）按集合校验且无集合时跳过、同一 HTML 在 `inspectHtmlPrototype`（直读）与 transfer `inspectHtml`（导入）两路有效性与错误结论一致。真实运行 `preflight` 于 rimo-rental 外部挂载页面（valid，2 条合理提醒）；`project -- health` 扫描全部真实项目无误报，并发现 demo 项目 client-3 外部原型目录 40 处指向「排班优化演示效果.html」的悬空互链及 1 处内联事件（真实缺陷，未修改 `projects/`）。全量门禁同 TRACE-01 记录，全部通过 | 完成 | **未做浏览器验收**（检查能力与 CLI/健康报告，无界面改动）；互链存在性依赖调用方提供文件集合，单文件粘贴导入场景按设计跳过该检查；demo 项目悬空互链属项目资料既存问题，请负责人安排修复源目录或链接 |
| 2026-08-28 18:27 | Claude | CTX-01 | `ai-context.js` 三项增强：（1）项目摘要与页面包携带 `theme`（manifest.theme 原样透出，空值归一为空对象）；（2）`createPageContextPackage` 的页面级 PRD 携带 `outline`（标题结构）、`content` 与 `contentAvailable`——正文不可用时 outline 显式为空、content 为空串，不编造章节；（3）新增 `extractDocumentSection`（围栏感知的行级标题扫描），组件级关联在页面包与追溯报告中输出 `section`（锚点对应章节标题 + 600 字以内正文摘录），内容不可用时返回 `contentAvailable:false` 加锚点回显。CLI 新增 `context` 命令：`npm run project -- context --project <id> --page <client/path 或 name 或 /p/ 全路径>` 输出单页交付上下文 JSON，`--output` 可写文件。UI 页面矩阵导出与 CLI 同调 `createPageContextPackage`，结构一致由同一核心函数保证 | 新增回归测试 4 项：theme 进入上下文与页面包、章节摘录提取（含锚点不存在返回 null）、内容不可用时 requirement 与关联 section 显式标记、追溯报告行携带 section；修正 fixture 的 documentSources 覆盖语义（空对象即全量清空）。真实运行 `context --project rimo-rental --page operation/workbench --output`：输出含主题四色、归档 PRD 的三级标题 outline 与正文、contentAvailable:true；`--page operation/corporate-login` 输出无关联页面并在 issues 中给出登记建议。全量门禁同 TRACE-01 记录，全部通过 | 完成 | **未做浏览器验收**（无界面改动，UI 导出结构随核心函数自动获得新字段）；「UI 导出、CLI 和 MCP 结果一致」验收项中 MCP 入口尚不存在无法本轮验证——UI 与 CLI 已由同一核心函数保证一致，MCP 一致性待 MCP-01 落地后复核，若届时结构有偏差以修正 MCP 适配层为准 |
| 2026-08-28 21:17 | Claude | MCP-01 | 新增 `packages/platform-mcp`：本地 MCP（Model Context Protocol）stdio 服务，零新依赖，协议层（initialize 握手回显协议版本、tools/list、tools/call、ping、通知静默、-32600/-32601/-32602 错误码、批处理拒绝、JSON 解析失败响应）与工具层分离，协议层为纯函数便于测试。六个只读工具全部调用 `project-core`：`list_projects`（有效/无效项目）、`get_project_overview`（主题、覆盖统计、含建议的追溯问题、关联候选）、`get_page_context`（单页交付上下文：页面级 PRD 正文+大纲、组件关联+章节摘录，支持 client/page、name、/p/ 全路径三种引用）、`list_documents`（元数据，无正文）、`list_associations`（页面级+组件级关联）、`list_health_issues`（健康问题，输出剔除 mounts 路径字段）。不提供写工具；仅 stdin/stdout 通信，不监听任何网络端口。安全边界：`redactLocalPaths` 在 JSON 序列化前对响应对象深遍历脱敏——已知根目录（workspace、projects-root、全部挂载根）连同后续相对路径段整体替换为 `<local-path>`，再以盘符正则兜底；期间修复两处真实缺陷：对序列化文本跑正则会因反斜杠转义截断产生非法 JSON，根目录前缀替换会残留文件名。配套下沉：CLI 的上下文装载逻辑（manifest、页面定义合并、文档清单与正文、页面/组件关联、遗留 page-prd-links.js、源文件可用性）迁入 `project-core/src/ai-context-loader.js` 的 `loadProjectAiContext`，CLI `loadContextInput` 改为委托，MCP 与 CLI、界面导出共用同一数据源；入口 `scripts/platform-mcp.mjs` + `npm run mcp:serve`，README 新增「本地 MCP Server」章节与客户端配置示例，ARCHITECTURE 分层图补充 platform-mcp | 新增 `tests/unit/platform-mcp.spec.js` 13 项：握手回显/未知版本回落/通知静默/tools/list 不泄露 run/未知工具与方法错误码、六个工具对临时项目夹具的输出断言（无效项目列出、覆盖统计、PRD 大纲与章节摘录、文档无正文、健康问题无 mounts 且无盘符路径、不存在页面返回工具级错误）、脱敏单元（盘符+POSIX 根替换、页面路由保留）、真实子进程 stdio E2E（spawn 脚本完成握手与 list_projects）。真实数据全链路：对当前 workspace 真实运行 MCP 进程，初始化、6 工具列出、3 个真实项目、rimo-rental 工作台页上下文（94 条大纲、主题、章节）、全部项目健康问题均正确返回，全部响应经正则核验零盘符路径泄露。CLI 重构等价性：重构前后 `trace --project rimo-rental` 输出除 generatedAt 外逐字节一致。`npm run lint:core`、`npm run typecheck`、`npm run test:unit`（38 文件 203 项）、`npm run project -- validate --projects-root examples`、`npm run build`、`git diff --check` 全部通过 | 完成 | **未做浏览器验收**（stdio 服务与 CLI，无界面改动）。CTX-01 的「UI/CLI/MCP 三处一致」验收项已闭环：三者同调 `createProjectContext`/`createPageContextPackage`，MCP 层仅做脱敏不改结构。已知边界：MCP 每次调用重新装载项目（本地磁盘可接受，未做缓存）；`sourceAvailability` 缺失源文件检查在 MCP 工具中生效（走共享加载器）；大项目 PRD 全文经 `get_page_context` 返回，体量由调用方按需取用；工作区存在 113 个既存未提交文件的 Prettier 行尾告警（CRLF checkout 环境噪音，提交内容为 LF，与本任务无关） |
