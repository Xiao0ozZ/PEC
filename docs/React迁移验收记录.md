# React 迁移一致性与切换门禁

更新日期：2026-08-13

## 1. 当前结论

React 平台已经覆盖首页、公共管理页、客户端 HTML 承载、文档中心、PRD、项目配置、路由管理和 HTML 导入导出闭环。用户已确认 `demo` 项目登记的 8 个 Vue SFC 页面不再使用，不属于 React 迁移范围，也不再阻挡正式入口切换。

默认开发、正式本地服务和生产构建入口已经切换到 React。旧 Vue 平台入口、旧 Vue 配置、旧 Vue E2E 和旧平台页面已删除，不再提供回退命令。Vue/Vite 仅作为历史 Vue 页面传输适配按需保留，不作为平台运行时。PRD 组件级可视化关联编辑已由用户明确取消，不属于正式功能；已有标记查看、页面级 PRD 关联和 PRD 侧窗继续保留。

本文件只记录平台迁移状态，不修改或要求自动改写 `projects/` 中的业务资料。

## 2. URL 与场景矩阵

| 场景             | URL                                | React 状态 | 静态验证                                             |
| ---------------- | ---------------------------------- | ---------- | ---------------------------------------------------- |
| 首页             | `/`                                | 已实现     | Ant 启动台、项目选择、URL 参数、本地记忆和管理快捷键 |
| 历史首页别名     | `/home-legacy`、`/variant-*`       | 已兼容     | 统一重定向正式首页                                   |
| 项目根入口       | `/p/{projectId}`                   | 已兼容     | 回首页并选中对应可见项目                             |
| 客户端登录       | `/p/{projectId}/{clientId}/login`  | 已实现     | 无可运行页面时停止假登录并显示明确状态               |
| 客户端页面       | `/p/{projectId}/{clientId}/{page}` | 已实现     | HTML iframe、参数、Hash、主题、PRD 查看和源码下载    |
| 文档中心         | `/p/{projectId}/docs`              | 已实现     | Markdown、目录、资源、Mermaid、旧链接和相对文档跳转  |
| 移动端           | `/p/{projectId}/mobile`            | 已实现     | 项目移动端入口承载                                   |
| 历史文档和移动端 | `/docs`、`/mobile`                 | 已兼容     | 按 `legacyRoutes` 项目重定向并保留参数与 Hash        |
| 历史客户端短链   | `/{clientId}/{page}`               | 已兼容     | 按 `legacyRoutes` 项目重定向并保留参数与 Hash        |
| 公共管理页       | `/tools/*`、`/components`          | 已实现     | React + Ant Design 单一组件体系                      |
| 隐藏项目         | 首页和所有直链                     | 已拦截     | 不进入客户端、文档或历史兼容入口                     |
| 未知地址         | 任意无效 URL                       | 已实现     | 显示 404，不再静默回首页                             |

## 3. 页面运行时清单

运行 `npm run audit:react-migration` 可重新扫描当前项目资料。命令在存在 HTML 索引缺失或无效项目包时返回失败；旧 Vue SFC 仅作信息记录，不修改、不运行，也不作为切换阻断。

当前只读扫描结果：

| 项目          | 客户端       | 可运行 HTML | Vue SFC | 状态                      |
| ------------- | ------------ | ----------: | ------: | ------------------------- |
| `demo`        | `admin`      |           1 |       5 | HTML 可运行，Vue 不使用   |
| `demo`        | `client-2`   |           0 |       3 | 无 React 页面，Vue 不使用 |
| `demo`        | `client-3`   |          40 |       0 | 通过                      |
| `rimo-rental` | `operation`  |          40 |       0 | 通过                      |
| `rimo-rental` | `enterprise` |          19 |       0 | 通过                      |
| `rimo-rental` | `client-3`   |           2 |       0 | 通过                      |

## 4. 已完成自动化验证

- React TypeScript 严格检查通过。
- React ESLint 通过。
- React 12 个测试文件、22 项测试通过。
- 全工程 25 个单元测试文件、107 项测试通过。
- `lint:core` 通过。
- `format:check:core` 通过。
- `project:validate` 通过：2 个有效项目、0 个无效项目。
- React 生产构建通过。
- HTML 托管页面和历史 Vue 输入兼容边界通过静态检查；平台构建不再生成 `dist-vue/`。
- 解除 Ant Design 强制单包后，首页预加载 JavaScript 的 gzip 总量由约 460 KB 降至约 219 KB；管理页组件继续按路由加载。

## 5. 尚未完成的验收

- `demo` 的 8 个 Vue SFC 页面不会转换、执行或修改；用户已确认这些页面没有使用价值。
- 尚未执行浏览器视觉、交互、键盘、焦点和真实 iframe 行为对照，因为当前约束是不默认启动浏览器验证。
- 正式 `/`、默认开发命令和默认生产构建已经切换到 React；旧 Vue 平台入口已删除。
- `platform-transfer` 仍保留历史 Vue SFC 的按需编译适配，这是导出/回导兼容能力，不是平台界面或新增页面开发入口。
