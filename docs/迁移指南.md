# HTML 原型接入工程规范

> React 是平台唯一运行入口。新页面统一使用规范 HTML，由工程外壳承载、扫描和管理；历史 Vue SFC 只作为项目资料兼容和页面传输工具的输入，不生成新的平台 Vue 页面。

## 1. 适用范围

本规范适用于将外部 HTML 原型接入项目包、平台页面或独立评审包。新原型必须从 `templates/html-prototype-page.html` 开始；历史 HTML 只能作为业务视觉和交互参照，不直接作为新模板复制。

接入目标是保留原型内容区的视觉、字段、模拟数据和交互，同时复用工程统一外壳、路由、主题和客户端配置。

模板已经标记可编辑区、锁定区、内容区、覆盖层、页面逻辑和页面样式；旧 HTML 页面仅作为视觉与交互参照，不能作为代码模板直接复制。

模板中的 `data-business-content` 是页面标题和子标题下方的自由业务区。默认统计卡、筛选和表格只是一种示例，可按需求整体替换为表单、详情、看板、控制台、地图、甘特图或其他业务布局；导入时保留该节点内部的实际内容，不按列表结构强制转换。

页面标题区按 `pageHeaderMode` 区分：一级列表、配置和看板通常使用 `standard`；二级详情页可使用 `back-only` 或 `none`。使用 `none` 时源 HTML 可以没有标题和子标题，导入器不得补造标题区。列表模板默认不增加“资料列表 + 总数”这类二级标题栏，筛选栏直接作为列表卡片顶部。

平台公共页使用 React 与 Ant Design；HTML 原型按源页面实际结构保留。`standard`、`back-only` 和 `none` 只描述标题区意图，导入器不会把业务内容强制转换成某一种布局。

## 2. 迁移前检查

1. 确认源 HTML、目标项目、客户端、菜单名称和目标路由。
2. 检查源页面 `#app` 内的 `aside`、`header`、`main` 和 `main` 后方同级节点。
3. 检查页面的弹窗、抽屉、表格、标签页、表单和覆盖层数量。
4. 检查页面逻辑、模拟数据、相对资源和页面间链接。
5. 检查页面专属 CSS、外部 CDN、图片和旧 `.html` 跳转。

不能只复制 `<main>`：许多原型把弹窗和抽屉放在 `</main>` 后方。正确内容范围是 `#app` 移除 `aside` 和 `header` 后的全部业务节点。

## 3. 目标文件

```text
projects/{project-id}/html-pages/{client-id}/<page>.html
```

- 一个 HTML 对应一个业务页面登记。
- 页面不得包含全局 `RouterView`、侧栏、顶栏、客户端选择、用户菜单或退出登录。
- 不建立空路由、占位页面或仅跳回旧 HTML 的中转组件。

## 4. 模板迁移

平台承载的 HTML 只保留业务内容：

```html
<main data-page-content data-business-content>
  <!-- 原内容区；按业务自由设计，不限定为卡片、表格或列表 -->
</main>
<!-- 同级弹窗、抽屉和覆盖层仍保留在模板规定区域 -->
```

禁止带入：

- `<aside>` 和原型侧栏。
- 原型全局 `<header>`。
- `flex h-screen` 等旧页面外壳。
- 平台运行时、框架入口和重复的客户端门户脚本。
- 不必要的全局事件和旧端切换脚本。

迁移后逐项核对按钮、字段、表格列、标签页、弹窗、抽屉、分页、筛选和空状态。不能为了让模板编译通过而删除不完整区块，应回到源 HTML 定点修复标签结构。

## 5. 页面逻辑

页面逻辑保留在模板的原生 JavaScript 区域，使用事件监听、状态对象和页面函数组织交互。不要引入平台 Router、平台外壳状态或项目工程源码。页面跳转使用相对 HTML 链接；需要保留查询参数和 Hash 时，直接写入链接。

如果 HTML 来自历史 Vue 页面，页面传输工具只在兼容导出或回导时按需处理其源码；这不改变新 HTML 页面的运行方式，也不会让历史页面重新成为平台运行入口。

## 6. 模拟数据

- 确需跨页面复用的企业、会员、车辆、据点、订单等静态数据放入所属项目包的 `projects/{project-id}/data`。
- 页面专属筛选、表单、选中项、弹窗状态和演示结果保留在页面组件内。
- 需要编辑共享数据时，复制为页面本地响应式状态，不能直接修改导入常量。
- 不因迁移重新设计业务字段或删除演示数据。

## 7. 样式迁移

1. 页面专属 CSS 放在 HTML 模板的页面样式区域，使用页面前缀或模板提供的语义变量。
2. 不重复定义全局字体、主色、侧栏、顶栏和 body 布局。
3. 内容区不使用固定 `100vh` 抢占外壳高度；页面内部滚动由内容区自身管理。
4. 原型中的边框、圆角、图标对齐和弹层层级必须保持完整。
5. 字体、演示图片和图标优先使用本地依赖或项目资源，不新增运行时外链。

## 8. 路由与菜单

每个项目的页面统一登记在 `projects/{project-id}/page-definitions.js`，共用外壳根据项目包生成 React 路由、菜单和页面标题。

新增页面优先复制 `templates/html-prototype-page.html`，通过页面导入工具或路由菜单管理写入 `html-pages/{client-id}`。禁止直接向平台 Router 或菜单数组追加项目业务页面。历史 `views/{client-id}` 下的 Vue 文件仅作兼容资料，不作为新页面目标。

客户端切换和退出登录统一回到：

- 客户端登录：`/p/{project-id}/{client-id}/login`
- 客户端页面：`/p/{project-id}/{client-id}/{page-path}`

## 9. 多语言

- 新增或迁移页面默认只开发简体中文，不要求同步繁中、英文，也不要求为了未来翻译预先改写全部模板。
- 只有产品负责人明确指定页面和目标语言后，才执行该页面的翻译任务。
- 新编写的全局公共组件如需多语言，使用 React i18next 结构化 key。
- 当前平台只提供简体中文；只有产品负责人明确指定页面和目标语言后，才增加对应资源。
- 动态插值、同词不同语境、图表文字或特殊组件无法使用精确映射时，再定点改为结构化 key。
- 模拟数据、姓名、手机号、车牌、订单号和业务记录默认不翻译。

当前没有独立的语言目录同步命令。多语言任务应直接补充 React i18next 资源，并为实际使用的页面增加测试。

## 10. 强制验收

每次 HTML 接入或批量修复后必须执行：

```powershell
npm run project -- preflight --file <html>
npm run audit:migration
npm run test:smoke
npm run build
```

高频演示页面、公共页面或高风险页面还必须加入 React E2E 视觉与交互检查。`audit:migration` 只检查 React 正式入口、项目页面来源和 HTML 运行边界，不执行历史 Vue SFC。

构建通过后再由产品负责人进行浏览器视觉验收。自动审计不能替代逐页确认间距、边框、圆角、颜色、图标位置和弹窗内容。

## 11. 常见错误

| 问题                   | 原因                                    | 处理                                        |
| ---------------------- | --------------------------------------- | ------------------------------------------- |
| 双侧栏或双顶栏         | 旧 `aside/header` 被带入                | 删除旧外壳，仅保留业务节点                  |
| 菜单点击空白或刷新白屏 | 页面来源缺失、脚本异常或页面定义无效  | 运行 `audit:migration`、项目校验和 `build`，修正真实错误 |
| 按钮点击无弹窗         | 只复制 `<main>`，遗漏同级弹窗/抽屉      | 回到 `#app` 范围补回所有业务同级节点        |
| 卡片边框和圆角丢失     | Preflight 关闭后缺少统一 border-style   | 使用当前 `base.css`，不在单页重复兜底       |
| 页面内容整体下移       | 页面重复实现顶栏或使用旧壳层高度        | 删除旧 header 和 `h-screen`                 |
| 弹窗样式不生效         | 覆盖层不在页面样式作用范围内             | 使用页面前缀选择器，并把覆盖层保留在模板区域 |
| 页面跳回错误地址       | 旧链接未改为相对页面链接                 | 使用相对 HTML 链接并保留参数和 Hash         |
| 页面首次打开较慢       | 页面资源过多或重复加载                   | 复用原 HTML 资源，避免嵌入完整工程运行时    |

## 版本记录

| 版本 | 日期       | 内容                                                                                       |
| ---- | ---------- | ------------------------------------------------------------------------------------------ |
| v1.0 | 2026-07-11 | 以统一 AppShell、动态路由、完整弹窗保留和自动迁移审计为准，替代旧单文件转换口径。          |
| v1.1 | 2026-07-12 | 增加样式隔离基线、本地资源约束、图表实例清理及异常兜底验收规则。                           |
| v1.2 | 2026-07-12 | 增加公共组件、Composables、无样式试点模式和组件治理审计规则。                              |
| v1.3 | 2026-07-12 | 增加页面生成器、统一页面注册表、ESLint、Prettier 和 CI 交付规则。                          |
| v1.4 | 2026-07-13 | 增加需求阶段可迁移 HTML 创建提示词、视觉参考边界、结构标记、导入清单和定案后单向迁移规则。 |
| v1.5 | 2026-07-13 | 建立可独立预览的 HTML 专用模板，固定机器可读的页面元数据、内容区、覆盖层、逻辑和样式边界。 |
