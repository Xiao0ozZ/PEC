# 合作通路租車券自动化测试接入资料

## 文档信息

| 项目 | 内容 |
| --- | --- |
| 用途 | 保存固定环境、账号、授权边界和仍需补充的测试条件 |
| 最近更新 | 2026-07-30 |

资料变化时直接修改原字段，不追加版本流水或执行结果。当前测试结果统一查看 `04_测试状态与交接记录.md`。

> 本文件保存测试环境的固定接入资料，仅限当前私有仓库使用。Token和验证码仍为运行时临时数据，不写入本文档、测试报告或缺陷证据。仓库改为公开、外发或开放第三方访问前，必须先移除本节凭证并更换全部测试密码。

## 1. 测试环境与账号

### 1.1 环境地址

| 项目 | 地址 |
| --- | --- |
| 管理后台 Swagger | [https://rentaltest.rimo.pro/api/manage/doc.html](https://rentaltest.rimo.pro/api/manage/doc.html) |
| APP API Swagger | [https://rentaltest.rimo.pro/api/client/doc.html](https://rentaltest.rimo.pro/api/client/doc.html) |
| 营运端 | [https://rentaltest.rimo.pro/](https://rentaltest.rimo.pro/) |
| 企业端 | [https://rentaltest.rimo.pro/enterprise/](https://rentaltest.rimo.pro/enterprise/) |
| 测试群组 | `RITIRD` |

### 1.2 营运端账号

| 项目 | 内容 |
| --- | --- |
| 登录接口 | `/api/manage/auth/login` |
| 验证码接口 | `/api/manage/auth/getPictureVerify` |
| 账号 | `1059468815@qq.com` |
| 密码 | `123456` |
| 群组 | `RITIRD` |

### 1.3 企业端账号

| 项目 | 内容 |
| --- | --- |
| 登录接口 | `/api/manage/auth/signingPlatformLogin` |
| 验证码接口 | `/api/manage/auth/getPictureVerify` |
| 账号 | `18851776533` |
| 密码 | `123456` |
| 群组 | `RITIRD` |

### 1.4 APP API账号

| 项目 | 内容 |
| --- | --- |
| 登录接口 | `/api/client/login` |
| 群组 | `RITIRD` |

```json
{
  "account": "D127550487",
  "appVerNo": "1",
  "channel": "1",
  "deviceId": "1",
  "password": "cNu9bqWGL6E=",
  "pushToken": "1"
}
```

运行脚本时，以上固定资料同步保存在 `automation/.env.test.local`。该文件用于程序读取；本文件用于人员接手和资料维护。Token由登录接口即时获取，不进行持久化。

## 2. 提测信息

| 项目 | 填写内容 |
| --- | --- |
| 测试版本或发布日期 | [待填写] |
| 功能上线时间 | [待填写] |
| 已知问题 | [待填写] |

## 3. 自动化授权

| 必须补充 | 填写内容 |
| --- | --- |
| 管理API自动登录方式 | 当前脚本可获取JPEG并保持Cookie，但本机无OCR引擎；现阶段由Codex识别。完全无人值守仍需固定验证码、测试绕过或安装OCR引擎 |
| 是否允许脚本自动创建批次、租車券和订单 | 已允许在测试环境自行选择现有数据执行 |
| 是否允许自动取消、取车、还车、启停和作废 | 已允许在测试环境执行；测试数据使用 `AUTO-CT-` 前缀 |
| 禁止操作范围 | 不操作正式环境；不删除原有数据 |
| 可执行测试时间段 | 任意时间 |

## 4. 安全测试数据

以下资料无法仅靠原型确定。ID和枚举值由测试脚本从接口查询，不要求人工抄写。

| 必须补充 | 填写内容 |
| --- | --- |
| 允许自动测试的合作企业名称 | 固定使用“租车券测试公司”；找不到该企业时阻断，不改用其他企业 |
| 允许使用的据点名称 | 当前选用“票劵合作据点”，据点类型“合作租車券專用” |
| 允许使用的测试车辆 | 当前库存提醒返回4辆适用车辆；批次创建阻断，尚未进入具体车辆查询 |
| 是否允许脚本改变测试车辆的空闲/占用/被管控状态 | 已允许在测试环境自行测试，优先只操作 `AUTO-CT-` 流程产生的占用 |
| 是否有不具备挂账能力的测试企业 | [有则填写名称；没有则对应负向用例标记阻塞] |

## 5. 会员与名单自动发放

| 必须补充 | 填写内容 |
| --- | --- |
| 当前APP测试会员是否已完成证件认证 | [待填写] |
| 是否允许测试期间调整该会员的证件资料和可用载具类型 | [待填写] |
| 如何验证“待发放 -> 认证后自动发放” | [提供第二测试会员，或说明如何给当前会员切换测试证件；无法提供则该链路标记阻塞] |
| 微电车测试车辆及会员资格是否已准备 | [待填写] |

## 6. 订单、时间与费用控制

| 必须补充 | 填写内容 |
| --- | --- |
| 如何把测试订单推进为取车、用车中和还车完成 | 已有APP `orderPickup`、`orderReturnTheCar`接口；待确认允许操作的测试车辆 |
| 如何制造逾时用车中和逾时结算 | 可创建短时间测试订单并等待，或提供时间模拟方式；待确认 |
| 如何给测试订单增加“其他费用” | 已确认管理端 `order/addFee`接口；待确认允许执行 |
| 如何查询合作企业挂账记录 | 已确认 `company/billingRecords`、`company/billInfo`和订单详情接口 |
| 如何确认未生成个人预授权、支付和退款 | 使用订单详情及 `order/preAuthList`、`order/payDeductionList`、`order/refundList` 独立回查 |

## 7. 测试数据清理

| 必须补充 | 填写内容 |
| --- | --- |
| 测试数据清理方式 | 默认停用并保留 `AUTO-CT-` 标识；如需其他方式请填写 |

## 8. 无需用户填写

- Swagger接口Tag、路径、请求和响应字段由Codex在功能上线后读取并映射。
- 页面路由和DOM定位由Codex从已登录页面读取。
- 名单异常文件、并发请求、导出解析和本地日期Bug说明由自动化脚本生成；页面截图和独立缺陷附件不生成。
- 跨群组和跨企业隔离由用户人工检查，不在本文件准备第二租户或第二企业账号。
